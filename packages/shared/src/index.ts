// @notez/shared - placeholder.
//
// Once the sync feature design is finalized, this is where the types
// shared between the desktop client, the web app, and the sync server
// will live: Note, NoteSummary, SyncEnvelope, end-to-end-encrypted blob
// shapes, etc. Keeping the package wired up now means the desktop and
// web Astro builds already resolve `@notez/shared` correctly when the
// first real types land.

export {};
