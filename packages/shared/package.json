{
  "name": "@notez/shared",
  "private": true,
  "version": "0.0.0",
  "description": "Shared types and helpers between desktop, web, and (future) sync server. Empty until the sync feature lands.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~6.0.3"
  }
}
